// src/styles/ContentContainer.js

import styled from "styled-components";

const ContentContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  margin-left: 20px;
  margin-right: 20px;
  gap: 26px 20px;
  margin-bottom: 20px;
`;

export default ContentContainer;
